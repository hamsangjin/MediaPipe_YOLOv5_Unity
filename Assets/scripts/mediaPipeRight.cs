using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mediaPipeRight : MonoBehaviour
{
    private List<Vector3> left;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void LateUpdate()
    {
        //left = new List<Vector3>(DataReceiver.Instance.getLeft(0));

        //left[0] = new Vector3(left[0].x, left[0].y, 0);
        //for (int i = 0; i < left.Count; i++)
        //{
        //    transform.GetChild(i).localPosition = left[i] * 2;
        //}

    }
}
